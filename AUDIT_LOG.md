# WWS Site — Quality & Growth Audit Log

Durable record for the **wws-site-quality-audit** scheduled agent (runs every 2h).
Each run: read this file + `git log`, pick the least-recently-covered focus area, ship ONE
small verified improvement (or log a proposal), append an entry below.

**Live:** https://wwslgc.collaborativeconceptsfl.com · **Repo:** this folder · **Deploy:** push to `main` → Vercel.

---

## Baseline — state as of 2026-06-10 (already built; don't re-do, don't break)

**Front end (`wwslgc/`)**
- `index.html` — landing: nav (How it works / What we handle / Plans / FAQ / Guides / Downloads / phone / Free Assessment), hero, standards strip, problem (enforcement-reality tone), the lane/differentiator, what-we-handle, top-15 violations gallery (16 image cards), how-it-works (4 bubble cards), plan builder, cost analysis, service areas, team, credentials, FAQ, resources/downloads (4 PDFs), assessment form (posts to `/api/lead`), footer (incl. Client portal link). Design tokens: primary `#13233f`, secondary `#c9a227`, surface `#f7f6f3`; Manrope headings, Inter body.
- `guides/` — 16 hazard guides + 5 topic guides (florida-recertification, fixed-ladder-2036, four-foot-rule, osha-penalties, inspection-checklist) + index with a "Free downloads" section. Generated by `_wws_blog_builder.py` (POSTS + TOPICS) — EDIT THE GENERATOR, not the HTML.
- `admin/` (CRM dashboard) + `admin/inspection.html` (inspection editor) — gated by Google login. `portal/` (client portal). `collateral/` — flyer, free-assessment offer, Subpart D checklist, 4×6 postcard PDFs (built by `_build_marketing.py` / `_build_collateral.py`).
- SEO: `sitemap-wws.xml` (23 URLs), `robots.txt`, GA4 `G-K9ZEXRRMCK`, Vercel analytics. GSC property verified.

**Back end (`api/index.py`, Flask on Vercel)** — all live & working:
- `POST /api/lead` (public) → stores to Upstash KV + emails via Resend (FormSubmit fallback).
- Admin (gated by `WWS_ADMIN_EMAILS`): `/api/admin/leads`, `/api/admin/lead/update`, `/api/admin/inspection*` + 4 PDF docs (reportlab) at `/api/admin/inspection/<id>/doc/<checklist|report|cert|proposal>`.
- Portal: `/api/portal/request`, `/api/portal/requests`. Google identity login via `/api/login/google?basic=1`.
- SiteCam hook (read-only consumer of `/api/ext/projects` + `/api/ext/projects/:id/photos`, `x-api-key`, config-gated by `SITECAM_BASE_URL`+`SITECAM_API_KEY`).

**DO NOT TOUCH** (working integrations / contracts / legal): KV storage, Google/MS OAuth, Resend, the SiteCam `/api/ext/*` consumer contract, admin/portal auth gating, any API request/response shape, and all compliance/liability language (La Gala is not an engineer; PE seals engineered work; "does not provide engineering services"; no "OSHA-certified"). Keep the `x-claude-source-repo` marker in every subdomain HTML file.

**Good first audit targets** (not yet hardened): JSON-LD structured data (LocalBusiness / FAQPage / Article), per-page meta-description uniqueness & length, internal-link density between guides, image alt-text coverage, mobile spacing on the landing, Lighthouse/perf (Tailwind CDN weight), heading-hierarchy validity, and broken-link/404 sweep across the full sitemap.

---

## Run log
<!-- newest first; each scheduled run appends an entry: date · area · finding · change(commit) or proposed · verified -->

## Backlog — needs Danny (risky; do NOT auto-apply)
<!-- structural redesigns, backend-logic changes, anything not safely verifiable -->
