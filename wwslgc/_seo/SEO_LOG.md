# WWS SEO Log

Working log for the La Gala WWS site (https://wwslgc.collaborativeconceptsfl.com).
Served from this repo's `wwslgc/` folder; auto-deploys on push to `main` via Vercel.
Guides + landing gallery + sitemap are GENERATED by `_wws_blog_builder.py` — never
hand-edit generated guide HTML; change the generator's `POSTS` / `TOPICS` / templates
and re-run `python _wws_blog_builder.py`. The city pages and the money page
(`roof-anchor-certification.html`, `*-roof-anchor-certification.html`) are
HAND-AUTHORED static HTML — edit those files directly.

## Hard guardrails (do not regress)
- `/` must stay 308 -> `/wwslgc`. Landing canonical + og:url = `.../wwslgc` (NEVER point canonical at `/`).
- Every wwslgc HTML page keeps `<meta name="x-claude-source-repo" content="dannybivins83-blip/collaborativeconcepts">`.
- Offer is branded "Comprehensive Assessment" — never "free"/"complimentary"/"low-cost" inspection.
- Engineering/PE work = "our licensed Florida PE partner"; La Gala does not provide engineering services.
- Accuracy: OSHA 2026 penalties up to $16,550 (serious / FTA-per-day), up to $165,514 (willful/repeat).
  RDS anchors §1910.27(b): annual qualified-person inspection + cert at least every 10 yrs.
  Fall-arrest gear §1910.140 (before each use); annual gear cadence is an ANSI Z359 recommendation, not OSHA.
  FL Milestone (FS 553.899): condos/co-ops 3+ stories, first at 30 yrs (local official MAY require 25 near salt
  water; old auto "within 3 mi of coast" rule REPEALED), then every 10. Miami-Dade 30/25, Broward 25. NEVER "40-year".
  Fixed ladders >24 ft §1910.28(b)(9): PFAS/ladder-safety system by Nov 18, 2036.

---

## 2026-06-25 — Audit + ship (Claude Opus 4.8)

### Audited
- Live-crawled all 46 URLs (root, landing, money page, 3 city pages, /guides, all 39 guides): **every one 200**.
- Sitemap (`sitemap-wws.xml`): all 39 guide files present; file set <-> sitemap diff = **0 orphans, 0 missing**.
- Redirect/canonical integrity: `/` -> **308** -> `/wwslgc` (intact). Landing canonical + og:url = `.../wwslgc` (correct, untouched).
- One H1 per page on all core pages. Marker meta present on every page.
- Structured data (live): landing = FAQPage + GeneralContractor; money page = Service + FAQPage + Offer + OfferCatalog + Audience;
  Miami = LocalBusiness + Service + BreadcrumbList + areaServed (Place/AdministrativeArea); FtL = LocalBusiness + Service + FAQPage + BreadcrumbList;
  WPB = LocalBusiness + Service + FAQPage + areaServed + Offer. Guides = Article + BreadcrumbList (via generator).
- Title lengths: ALL core pages over 60 (71–107 raw incl. `&amp;`). Logged as backlog (kept titles as-is this run — keyword-bearing, lower risk than chasing 60 exactly).
- Meta description lengths: landing + money + 3 city pages all **230–266 chars** (truncating in SERP, CTA lost). FIXED this run.
- Guide metas: 142–148 chars, accurate — generator truncation logic is healthy, no action.

### Shipped (commit — see git log `WWS SEO:`)
1. **Tightened 5 over-long meta descriptions to <=155** (keyword front-loaded, unique, CTA preserved, accuracy-safe):
   - `index.html` 230 -> 149
   - `roof-anchor-certification.html` 261 -> 150
   - `miami-roof-anchor-certification.html` 266 -> 153
   - `fort-lauderdale-roof-anchor-certification.html` 246 -> 149
   - `west-palm-beach-roof-anchor-certification.html` 265 -> 150
2. **Money-page og:description**: replaced off-brand "low-cost initial inspection" -> "comprehensive assessment" (brand + guardrail alignment).
3. **Sitemap lastmod auto-freshness**: generator `LASTMOD` was hardcoded `2026-06-09`; now `datetime.date.today()` so every regen stamps the real build date. Regenerated sitemap -> lastmod `2026-06-25`.
- Re-ran `python _wws_blog_builder.py` (compile OK): 39 posts + index + landing gallery + sitemap regenerated; guide HTML content unchanged.

---

## Prioritized backlog (for the daily wws-seo-agent)

**P1 — high value, safe**
1. **Tighten core-page <title> to <=60 chars** (currently 71–107). Suggested rewrites (keep primary kw front-loaded; brand can be shortened to "| La Gala"):
   - money page: "Roof Anchor Certification & Load Testing | South Florida" (drop the long "Window-Washing System" clause from title; keep it in H1/body).
   - WPB: "Roof Anchor Certification West Palm Beach | La Gala" .
   - FtL: "Roof Anchor Certification Fort Lauderdale | La Gala".
   - landing: "OSHA Walking-Working Surfaces Compliance | La Gala".
   These are hand-authored files — edit `<title>` directly; verify <=60 with display-length (treat `&amp;` as 1 char) before commit.
2. **Add FAQPage schema to the Miami page** — it has LocalBusiness + Service + BreadcrumbList but (unlike FtL & WPB) **no FAQPage**. Mirror the FtL FAQ block, swap in Miami-specific Q&A (Miami-Dade 30/25 milestone, salt-air, Brickell/Sunny Isles). Keep accuracy guardrails.
3. **Add CollectionPage + BreadcrumbList JSON-LD to `/guides` index** (currently no JSON-LD). Do it in the generator's `build_index()` so it regenerates.

**P2 — content/keyword gaps (only via generator TOPICS, never hand-edit generated HTML)**
4. Long-tail guide opportunity: **"Roof anchor load test cost Florida"** / "how much does roof anchor certification cost" — high commercial intent, currently uncovered. Add as a TOPICS entry (pricing-cautious, no hard $ promises).
5. Long-tail: **"OSHA RDS certification vs Florida milestone inspection"** — disambiguation page; strong condo/HOA intent, ties the two regimes together (we own both topics separately but not the comparison).
6. Consider a **Boca Raton** or **Aventura/Sunny Isles** sub-city page if GSC shows impressions clustering there (don't build speculatively — gate on Search Console data).

**P3 — interlinking / polish**
7. **Money/city pages -> relevant guides**: verify each city page links to `roof-anchors`, `florida-recertification`, `rope-descent-recertification`, and the relevant milestone guide. Add an "Related guides" block if missing (hub-and-spoke completeness).
8. **Guides -> money page**: ensure roof-anchor-family guides (roof-anchors, roof-anchor-recert-frequency, rope-descent-recertification, miami-dade-roof-anchors) link to `/roof-anchor-certification` in-body, not just via the CTA.
9. **Image alt audit on hand-authored pages**: spot-check city/money page `<img>` alts are descriptive + keyword-relevant (generator already sets guide img alt = title_plain; OK).
10. **og:image per city page**: all three city pages reuse `roof-anchors.jpg`. Low priority, but a city-distinct social image would help CTR on shares.

**Monitoring**
- After any deploy, re-verify `/` still 308 -> `/wwslgc` and landing canonical still `.../wwslgc` (the old GSC "redirect error" came from canonical pointing at the redirecting `/`).
- Watch GSC for the 5 tightened metas to re-index; confirm no description truncation remains.
