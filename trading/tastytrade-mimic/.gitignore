# Runtime state + local signal drops — never commit
state.json
signals.json
trades.jsonl
positions.json
KILL_SWITCH
.env
*.env
# Deploy secrets — filled from env.sh.example on the VM, never committed
env.sh
