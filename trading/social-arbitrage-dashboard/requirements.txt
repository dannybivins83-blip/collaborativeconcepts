# Lower bounds only (not hard-pinned) so security patches flow through.
# streamlit>=1.28 for st.rerun() (renamed from experimental_rerun in 1.27) and
# a modern pandas Styler.format() + st.form/tabs feature set.
streamlit>=1.28
pytrends>=4.9
pandas>=2.0
yfinance>=0.2.28
requests>=2.31
