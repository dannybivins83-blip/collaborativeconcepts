{
  "name": "collaborativeconcepts-wws-css",
  "version": "1.0.0",
  "private": true,
  "description": "Static Tailwind CSS build for the WWS public pages (replaces the runtime CDN).",
  "scripts": {
    "build:css": "tailwindcss -i ./src/tailwind-input.css -o ./assets/wws.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.17",
    "@tailwindcss/forms": "^0.5.9",
    "@tailwindcss/container-queries": "^0.1.1"
  }
}
